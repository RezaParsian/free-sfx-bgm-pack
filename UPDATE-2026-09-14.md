# 🔄 بسته‌ی همگام‌سازی — ۲۰۲۶-۰۹-۱۴

مخزن گیت‌هاب شما (`RezaParsian/free-sfx-bgm-pack`) با نسخه‌ی **قبل از پک تایپ/ترمینال** push شده.
این بسته فقط شامل فایل‌های **جدید و تغییرکرده** است.

## محتویات بسته

| مسیر | وضعیت |
|---|---|
| `audio-pack/sfx/type_*.wav` + `terminal_bell` + `prompt_tick` + `scroll_ratchet` | 🆕 ۹ افکت تایپ/ترمینال (WAV) |
| `audio-pack/bgm/bgm_terminal_flow.wav` | 🆕 ترک جدید BGM برای جلسه‌های ترمینال |
| `audio-pack/mp3/sfx/…` + `mp3/bgm/bgm_terminal_flow.mp3` | 🆕 نسخه‌ی MP3 همان‌ها |
| `audio-pack/measured.csv` | ✏️ به‌روز (۴۹ فایل) |
| `README.md` | ✏️ شمارنده‌ها ۴۰/۹ + لینک‌های پلیر صوتی واقعی (برنچ `master`) |
| `START-HERE.fa.md` | ✏️ شمارنده‌ها و جدول‌ها |
| `guides/guide.html` · `guides/README.md` · `guides/audio-library.xlsx` | ✏️ دسته‌ی «تایپ و ترمینال» + الگوی ویدیوی آموزش کد/لینوکس |
| `social-preview.png` | ✏️ شمارنده‌های ۴۰/۹ + واترمارک آدرس مخزن |
| `sfx-lab/*.py` | ✏️ ژنراتورهای به‌روز |
| `.gitignore` | 🆕 (در push اول جا افتاده بود) |

## دستورات

```bash
# ۱) بسته را در ریشه‌ی مخزن extract کن (روی فایل‌های قبلی بازنویسی می‌شود)
cd free-sfx-bgm-pack
unzip -o sync-update-2026-09-14.zip

# ۲) حذف لایسنس تکراری (GitHub موقع ساخت مخزن LICENSE.md ساخته؛
#    فایل LICENSE خودِ پروژه کامل‌تر است: CC0 برای صدا + MIT برای کد)
git rm LICENSE.md

# ۳) کامیت و push
git add -A
git commit -m "feat: typing/terminal SFX pack + terminal_flow BGM, sync docs & branding"
git push
```

## بعد از push — تنظیمات مخزن (فقط یک بار)

1. **Social preview:**
   `https://github.com/RezaParsian/free-sfx-bgm-pack/settings`
   → بخش **Features → Social preview** → آپلود `social-preview.png`

2. **Explanation (About):** در صفحه‌ی مخزن، چرخ‌دنده‌ی کنار About:
   ```
   40 sound effects + 9 BGM tracks synthesized from code (DSP) — CC0, no attribution,
   no Content ID claims. Typing/terminal pack for coding & Linux tutorials.
   Full Persian placement guide included.
   ```

3. **Topics:**
   `sound-effects` `royalty-free` `cc0` `bgm` `dsp` `audio-synthesis` `python` `numpy`
   `content-creator` `youtube` `video-editing` `linux` `terminal` `persian`

4. **(اختیاری) GitHub Pages برای راهنمای بصری:**
   Settings → Pages → Source: `Deploy from a branch` → branch `master` / folder `/ (root)`
   → بعد از deploy، آدرس `https://rezaparsian.github.io/free-sfx-bgm-pack/guides/guide.html`
   را به‌عنوان **Website** در About بگذار. (guide.html تک‌فایله و بدون منبع خارجی است،
   پس روی Pages بدون مشکل کار می‌کند.)

## نکته درباره‌ی حجم مخزن

WAVها داخل مخزن شما commit شده‌اند (حدود ۶۲MB) — برای یک مخزن عمومی مشکلی نیست
(سقف گیت‌هاب ۱۰۰MB **برای هر فایل** است و بزرگ‌ترین فایل شما ۸MB است).
فقط مراقب باش: هر بار WAVها را بازتولید و commit کنی، یک کپی کامل دیگر به history
اضافه می‌شود. اگر روزی مخزن سنگین شد: `git rm -r --cached audio-pack/sfx audio-pack/bgm`
و فقط MP3ها را نگه دار (با یک دستور بازتولید می‌شوند).
